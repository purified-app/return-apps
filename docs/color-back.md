# ColorBack

Camera eyedropper. Contract **v1**: `value=#rrggbb` + `format=color.hex` plus `rgb=r,g,b`. Default delivery: **query**.

See [contract-v1.md](./contract-v1.md).

```bash
bun run start:color-back   # :4206
```
