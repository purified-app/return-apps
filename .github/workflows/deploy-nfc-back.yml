name: Deploy nfc-back

on:
  push:
    branches: [main]
    paths:
      - 'projects/nfc-back/**'
      - 'projects/shared-ui/**'
      - 'package.json'
      - 'bun.lock'
      - 'angular.json'
      - 'tsconfig.json'
      - '.github/workflows/deploy-nfc-back.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-nfc-back
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14
      - run: bun install --frozen-lockfile
      - run: bun run build:nfc-back
      - name: Deploy to nfc-back Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          personal_token: ${{ secrets.DEPLOY_GITHUB_TOKEN }}
          external_repository: purified-app/nfc-back
          publish_dir: dist/nfc-back/browser
          publish_branch: gh-pages
          force_orphan: true
          commit_message: 'Deploy nfc-back from return-apps'
