name: Deploy map-pick-back

on:
  push:
    branches: [main]
    paths:
      - 'projects/map-pick-back/**'
      - 'projects/shared-ui/**'
      - 'package.json'
      - 'bun.lock'
      - 'angular.json'
      - 'tsconfig.json'
      - '.github/workflows/deploy-map-pick-back.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-map-pick-back
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14
      - run: bun install --frozen-lockfile
      - run: bun run build:map-pick-back
      - name: Deploy to map-pick-back Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          personal_token: ${{ secrets.DEPLOY_GITHUB_TOKEN }}
          external_repository: purified-app/map-pick-back
          publish_dir: dist/map-pick-back/browser
          publish_branch: gh-pages
          force_orphan: true
          commit_message: 'Deploy map-pick-back from return-apps'
